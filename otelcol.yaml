receivers:
  otlp:
    protocols:
      http:
        endpoint: go-rates-collector:4318

processors:
  batch:

exporters:
  otlp/jaeger:
    endpoint: go-rates-jaeger:4317
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlp/jaeger]